# Tasks

- [x] Task 1: 重写 `generateShareImage(tarot)` 函数
  - [x] SubTask 1.1: 重写背景层：保留现有渐变，增加 3-4 个大尺寸径向辉光色块（紫、金、蓝混合），模拟星云效果
  - [x] SubTask 1.2: 重写星场层：远景小星 200+颗 + 中景星 60颗 + 近景闪光大星 8-12颗（含十字光芒）
  - [x] SubTask 1.3: 绘制中央装饰区：外框 + 内框 + 四角 L 形角标 + 调用 `arcanaGlyphs[tarot.name]` 绘制大型符号 + 双层圆环 + 12条放射线
  - [x] SubTask 1.4: 优化文字层排版：增加问题引号、调用 `buildReading(tarot).keyword` 展示关键词标签
  - [x] SubTask 1.5: 美化底部装饰：双层水平线 + 菱形点缀 + 品牌文字

# Task Dependencies
- 无依赖，单一任务可直接执行
