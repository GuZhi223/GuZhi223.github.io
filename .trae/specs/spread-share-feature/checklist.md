# Checklist

- [x] `generateSpreadShareImage(drawnCards, userQuestion)` 函数已定义，位于 `generateShareImage` 之后
- [x] 分享图片画布尺寸为 1080×1920 像素
- [x] 星空背景复用现有逻辑（渐变底色 + 星云 + 多层星场）
- [x] 顶部包含装饰边框（四角 L 形 + 双层矩形）
- [x] 顶部显示 "TAROT · 星门塔罗" 品牌标题
- [x] 用户问题文本以「」引号装饰显示在品牌标题下方
- [x] 三张牌横向排列，每张牌有深色圆角矩形底板
- [x] 每张牌的 `arcanaGlyphs[name]` 符号正确绘制并居中
- [x] 逆位牌的符号区域旋转 180°
- [x] 三张牌之间有竖向装饰渐变分隔线
- [x] 每张牌下方显示位置标签（过去/现在/未来）
- [x] 每张牌下方显示牌名（粗体）
- [x] 每张牌下方显示正/逆位标识（正位金色 ▲ / 逆位粉色 ▼）
- [x] 释义文字使用 `wrapText` 自动换行，不超出牌列宽度
- [x] 底部整体总结区域包含装饰分隔线和菱形点缀
- [x] 总结区域展示三张牌的关键词汇总
- [x] 底部品牌区域包含装饰线 + 菱形 + "星门塔罗" + 日期
- [x] `showSpreadResult()` 中 `drawn.length >= 2` 时创建了分享按钮
- [x] 分享按钮文案为 "✦ 分享牌阵"，class 为 `btn btn-ghost`
- [x] 分享按钮点击后调用 `generateSpreadShareImage` 生成图片
- [x] 生成的图片通过 `showSharePreview(canvas)` 正确显示在预览弹窗中
- [x] 预览弹窗中保存图片按钮可正常下载图片
- [x] 单张牌分享功能（`count === 1` 时使用 `generateShareImage`）不受影响
- [x] 分享图片在 360px 宽度下文字清晰可读（移动端适配）
- [x] 未引入外部图片资源，所有图形通过 Canvas 2D API 程序化绘制
